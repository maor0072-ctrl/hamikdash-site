# מצב הפרויקט - אתר hamikdash.co.il

> תיק מסירה לסשן "אתר המקדש". נכתב ב-19 באוגוסט 2026. קרא אותי לפני שאתה עונה משהו על הפרויקט הזה.

## המטרה
להעלות לאוויר אתר לעמותת אורות האמונה - בני משה (הרב יעקב מאור), על הדומיין hamikdash.co.il.
הבעלים ביקש במפורש: **התוכן הראשוני לא קריטי, העדיפות היא שהאתר יעלה לאוויר.** תוכן מדויק נעדכן אחר כך.

## מה כבר בנוי (הושלם)
אתר סטטי מלא ב-`build/hamikdash-site/` - HTML ו-CSS בלבד, בלי מסגרות ובלי שלב בנייה:
`index.html` (בית), `about.html` (אודות), `torah.html` (תוכן תורני), `donate.html` (תרומה), `contact.html` (יצירת קשר), `assets/style.css`, וכן `_headers`, `robots.txt`, `sitemap.xml`.

עברית RTL, מותאם נייד, גווני קרם וזהב, גופן Heebo. נבדק בשרת מקומי: כל חמשת העמודים מחזירים 200 ואין קישור פנימי שבור.

הקוד דחוף לרפוזיטורי פרטי בגיטהאב: `maor0072-ctrl/hamikdash-site` (ענף main).
התיקייה נמצאת ב-`.gitignore` של ה-AIOS בכוונה - היא רפוזיטורי עצמאי משלה.

## עובדות שנבדקו בפועל (לא להניח מחדש)
- הדומיין הוא **hamikdash.co.il** (הבעלים כתב בטעות hamikdask - כתובת כזאת לא קיימת).
- שני הדומיינים של הבעלים, hamikdash.co.il וגם reshimu.co.il, יושבים על אותו שרת: **62.219.78.231** (Plesk של LiveDNS, Windows/IIS), ושניהם מגישים דף ריק של 476 בתים מיוני 2024.
- לשני הדומיינים יש **שרת דואר פעיל** אצל LiveDNS (MX ל-mail.<domain>, SMTP עונה). **הדואר קריטי** - כתובות המייל מודפסות על הספרים של הבעלים. אסור לשבור אותן.
- אין לדומיין רשומת SPF, ולכן מייל שנשלח ממנו עלול להיחשב ספאם. שווה לטפל בהמשך.
- reshimu.co.il הוא הדומיין הישן. hamikdash.co.il הוא החדש.

## ההחלטה שהתקבלה על הפריסה
נבחנו שלוש דרכים. **הדרך שנבחרה: העלאת האתר לאחסון הקיים ב-LiveDNS דרך FTP.**
הנימוק: האתר עולה לאוויר מיד, הדואר לא זז בכלל, אין שינוי DNS, ואין עלות נוספת (האחסון כבר משולם).
Cloudflare Pages נשקל ונדחה **לשלב מאוחר יותר** בלבד, כי הוא דורש העברת שרתי שמות, וזה נוגע ברשומות הדואר.
אם בכל זאת עוברים ל-Cloudflare בעתיד: חובה להעתיק קודם את רשומת ה-MX ואת רשומת `mail` שמצביעות ל-62.219.78.231, ורק אז להחליף שרתי שמות.

## מה חוסם עכשיו (הצעד היחיד)
**חסרים פרטי FTP לאחסון של hamikdash.co.il** - שרת, שם משתמש וסיסמה, מהפאנל של LiveDNS (או גישה ל-Plesk).
הומלץ לבעלים לפתוח משתמש FTP ייעודי במקום להשתמש בראשי, ולהחליף סיסמה מיד אחרי ההעלאה.
ברגע שהפרטים מגיעים - ההעלאה עצמה היא דקה, ואפשר להשתמש ב-`ftplib` של פייתון (אין Node ואין wrangler על המכונה הזאת).

## אזהרת אבטחה פתוחה
הבעלים שלח בטלגרם פרטי התחברות חיים למערכת SafeCMS של reshimu.co.il (משתמש admin, סיסמה, ומפתח הצפנה). הם חשופים כעת ביומן הבוט המקומי, בטלגרם ואצל Anthropic.
**פעולות פתוחות:** להחליף שם סיסמה, ולהציע למחוק את השורה מיומן הבוט (`data/command/bot.log`). הבעלים טרם אישר את המחיקה.
אותם פרטים שייכים ל-reshimu, **לא** ל-hamikdash, ולכן הם לא פותרים את חסימת ה-FTP.

## פריטי תוכן פתוחים (מסומנים באתר עצמו בתיבות צהובות, class="todo")
טופס נדרים פלוס בעמוד התרומה (מספר מוסד + קוד הטמעה) · מספר עמותה, אישור ניהול תקין, אישור סעיף 46 · כתובת מייל רשמית (הבעלים ציין שיש כתובת שמתחילה ב-info ואולי אחת בשם maor - צריך לאמת) · אישור שמספר הוואטסאפ 052-839-5189 מיועד לפרסום פומבי · פסקת ביוגרפיה על הרב יעקב מאור · לוגו ותמונות (כרגע אין באתר שום תמונה).

## מה נשאר לעשות בקוד
לוגו ואייקון דפדפן (SVG), דף שגיאה 404, והרחבת התוכן מעבר לשלד. אלה לא חוסמים עלייה לאוויר.
